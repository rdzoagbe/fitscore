// Root app is still the production Vite app.
// Keep this config intentionally empty so Vercel/Vite does not try to load
// Tailwind plugins that belong to the isolated Next.js rebuild.
const config = {
  plugins: {}
}

export default config
